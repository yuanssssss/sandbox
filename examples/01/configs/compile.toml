[process]
argv = ["/usr/bin/make", "-f", "examples/01/Makefile", "all"]
cwd = "."
env = ["LANG=C", "PATH=/usr/bin:/bin"]
clear_env = true

[limits]
wall_time_ms = 10000

[io]
artifact_dir = ".sandbox-runs/examples-01/compile"

[security]
seccomp_profile = "default"

[filesystem]
enable_rootfs = false

# 这个编译配置优先保证示例简单可跑：
# - 关闭 rootfs，让 `make` 可以直接把产物写回 `examples/01/build`
# - 仍然保留 supervisor、超时、审计日志和 seccomp 默认配置
