[process]
argv = ["/bin/echo", "hello from sandbox"]
cwd = "."
env = ["LANG=C", "PATH=/usr/bin:/bin"]
clear_env = true

[limits]
wall_time_ms = 1000
# 默认示例不主动启用 cgroup 限额，便于在普通开发环境直接运行。
# 如需启用 CPU cgroup 限额：
# cpu_time_ms = 1000
# 如需启用 cgroup v2：
# memory_bytes = 134217728
# max_processes = 32

[io]
artifact_dir = ".sandbox-runs/minimal"

[security]
seccomp_profile = "default"

[filesystem]
enable_rootfs = true
# 这个模板优先保证“普通开发环境先跑通”。
# 如果你需要更强隔离，请直接参考同目录下的 `strict.toml`。
enter_user_namespace = false
enter_mount_namespace = false
enter_pid_namespace = false
enter_network_namespace = false
enter_ipc_namespace = false
apply_mounts = false
chroot_to_rootfs = false
drop_capabilities = true
work_dir = "/work"
tmp_dir = "/tmp"
executable_bind_paths = ["/bin", "/usr/bin"]
# readonly_bind_paths = ["/abs/path/to/input.txt"]
# output_dir = "/output"
